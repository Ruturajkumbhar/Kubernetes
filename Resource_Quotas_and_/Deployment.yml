apiVersion: apps/v1
kind: Deployment

metadata:
  name: nginx-deployment
  namespace: resource-demo

spec:
  replicas: 2

  selector:
    matchLabels:
      app: nginx

  template:
    metadata:
      labels:
        app: nginx

    spec:
      containers:
        - name: nginx
          image: nginx:latest

          resources:
            requests:
              cpu: "200m"
              memory: "200Mi"

            limits:
              cpu: "500m"
              memory: "500Mi"

          ports:
            - containerPort: 80

