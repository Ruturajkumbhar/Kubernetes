apiVersion: v1
kind: Pod

metadata:
  name: high-resource-pod
  namespace: resource-demo

spec:
  containers:
    - name: nginx
      image: nginx

      resources:
        requests:
          cpu: "2"
          memory: "3Gi"

        limits:
          cpu: "3"
          memory: "4Gi"

