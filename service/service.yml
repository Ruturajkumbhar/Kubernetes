apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: nginx
spec:
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80        # Service port (accessible inside cluster)
      targetPort: 80  # Pod container port
  type: ClusterIP


